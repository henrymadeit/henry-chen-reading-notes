---
import type { Article } from '../lib/articles';
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from './ArticleCard.astro';
const { title, description, articles, heroImage } = Astro.props as {
  title: string;
  description: string;
  articles: Article[];
  heroImage?: string;
};
---
<BaseLayout title={`${title}｜Henry Chen 的隨筆`} description={description}>
  <header
   class:list={['page-header', 'wrap', { 'page-header-hero': heroImage }]}
    style={heroImage ? `--hero-image: url('${heroImage}')` : undefined}
  >
  <p class="eyebrow">Section</p>
  <h1>{title}</h1>
  <p>{description}</p>
</header>
  <section class="wrap section"><div class="card-grid">{articles.map((article) => <ArticleCard article={article} />)}</div></section>
</BaseLayout>
